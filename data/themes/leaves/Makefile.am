themedir = $(datadir)/xsplashaa/leaves

theme_DATA = \
    leaves.engine \
    distrib-logo.png \
    logo.png \
    restart-press.svg \
    restart-release.svg \
    shutdown-press.svg \
    shutdown-release.svg

EXTRA_DIST = $(theme_DATA)

